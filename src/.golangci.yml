# Run only errcheck so CI passes; enable more linters as issues are fixed.
run:
  timeout: 5m
linters:
  enable:
    - errcheck
  disable-all: true
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
